# BOT Cartera CEDEARS — Project State

_Last updated: 2026-09-14 (end of session)_

## Objective

**Single mandate (redefined 2026-09-12, supersedes any earlier "capital preservation is primary" framing): build a CEDEAR portfolio, using the capital available, that reaches or exceeds $20,000 additional USD by 2028-09-11** (24 months from 2026-09-11), on top of current NAV (~$49,087). Risk management is not a competing constraint against this goal -- it exists in service of reaching it reliably. G4/Bear-case discipline, correlation-awareness and fail-closed data handling all still apply; what changed is that sizing is no longer capped by an independent "preserve capital first" ceiling, and every new-buy proposal must be evaluated against the whole current portfolio (existing holdings' own risk + correlation), not in isolation. Weekly pace is expected to fluctuate with what the market actually offers -- the system does not fabricate or relax fundamental analysis to hit a return number on schedule; see `goal_tracking` for the running pace metric.

The pipeline must not manufacture deployment candidates. `NO_NEW_DEPLOYMENT` remains valid unless downstream stages, using consistent lineage and verified data, justify otherwise.

## Current production architecture (post Fase 2, 2026-09-11)

```
Comafi universe -> BYMA/Caja negotiability validation -> real eligible universe (~235-304 instruments, count is dynamic)
  -> Valuation (FULL universe, broad-universe Finnhub consensus scenario)
  -> Research value-first ranking (equities ranked by value_score/fundamentals;
     technical screening_score is a binary entry-timing gate only; ETFs keep a
     separate technical-only ranked track) -> immutable Top-30
  -> Deep Scenario Review V2.2 (sector-aware: 7 archetypes -- CORPORATE/FINANCIALS/
     ENERGY/UTILITIES/BASIC_MATERIALS/CONSUMER_NON_CYCLICAL/CONSUMER_CYCLICAL)
  -> G4 Cash Hurdle -> Risk -> Committee (allocator ranks G4_PASS by risk_adjusted_er,
     risk-budget fill; goal_tracking reports pace vs the $ objective)
```

**This is a change from the architecture description in the previous version of this file** (which had Research/technical-screening picking the Top-30 *before* Valuation ran on just those 30). That old order was found, and fixed, this session — see "Fase 2" below.

Key rules (unchanged):
- Valuation now runs on 100% of the canonical eligible universe (used to be Top-30 only).
- Deep Scenario Review / G4 remain limited to the immutable Top-30, selected by the value-first ranking.
- G4 requires Bull/Base/Bear scenarios, probabilities, net expected return, downside, costs, uncertainty penalty and explicit comparison versus cash.
- No artificial deletion/replacement of Top-30 names to force G4 success.
- Lineage must remain pinned across stages.
- Universe counts must be dynamic, never hardcoded to historical fixed values (316/305/304 — the exact count moves as the canonical universe is refreshed; treat any hardcoded count as a bug).
- IWDA remains an explicit mandate exception and must remain inside the universe with normal screening/scoring and traceability.

## Session 2026-09-11 summary (this session)

Started from a working but economically stuck pipeline: G4 had **never produced a single PASS candidate** despite passing all its own unit tests. Root-caused and fixed a chain of real economic/methodology bugs, each validated against real market data via GitHub Actions before merging, in this order:

1. **Goals-based hurdle tracking** (PR #12) — `src/portfolio/goals.py`, `src/orchestration/build_investment_committee.py`: tracks required annual return toward the $20k/24mo objective; informational, never gates.
2. **ENERGY archetype missing bull-ordering floor** (PR #13) — `deep_scenario_engine.py::_energy_targets`.
3. **CORPORATE bear case over-harsh (mechanical-only) + megacap/tech ranking tilt** (PR #15).
4. **Bear downside backstop loosened -15% -> -40%** (PR #16) — the old -15% floor was found (via `g4_sensitivity_audit.py` against real data) to reject virtually every real equity regardless of quality; real risk limiting now lives in the allocator's stress budget, not a per-name absolute veto.
5. **FX/regulatory-stress individual veto removed** (PR #17) — still computed and fed to the allocator's stress budget, but no longer an individual PASS/FAIL gate (a CEDEAR holder can hedge via canje/conversion to the underlying).
6. **Winsorize consensus low + growth-adjusted P/E cap for CORPORATE** (PR #18) — symmetric winsorization of analyst high/low targets; P/E cap now scales with growth instead of a flat 25x/30x (was structurally suppressing Bull upside for megacap/growth names like NVDA, MSFT).
7. **Widened confidence-to-Bull probability range + removed double-counted per-name margin stacking** (PR #19) — `bull_min/bull_max` widened 15-27% -> 12-32%; `minimum_margin_over_hurdle` 2% -> 0.5%; zeroed `dynamic_equity_margin`'s `base_equity_risk_buffer`/`uncertainty_buffer_max` (both were double-charging risk already priced elsewhere). **This produced the first G4_PASS candidates all session: 4/30 (DAL, NVDA, GE, MSFT).**
8. **Extended consensus bear-blend from CORPORATE to FINANCIALS/ENERGY** (PR #20) — for consistency; confirmed firing correctly on real FINANCIALS names (AXP, WFC) in production.
9. **Fase 2 cutover** (PR #21) — the big one: reordered the production pipeline so Valuation runs on the full universe *before* Research picks the Top-30, and Research selects equities by fundamentals (`value_score`) instead of technical momentum, with technical screening demoted to a binary entry-timing gate. Motivated by hard evidence: production's old technical-only Top-30 **did not even contain** the 4 tickers that pass G4 (DAL, NVDA, GE, MSFT) — the momentum/trend screen was structurally excluding exactly the names the valuation engine could validate as attractively priced.

**Validated on real production data on `main` after merge**: full chain `weekly_screening -> valuation_scenarios -> g4_cash_hurdle -> decisional_risk -> investment_committee` ran green end-to-end with **zero code changes needed downstream of Research** (g4/risk/committee untouched). Result: **4/30 G4_PASS** (DAL +3.87pp, NVDA +3.27pp, GE +1.56pp, MSFT +1.11pp above hurdle) — exact match to the shadow prediction. Committee's final decision this cycle was `HOLD_CASH_NO_ACTION` — **not a regression**, just the pre-existing fail-closed governance rule refusing to deploy while any Top-30 ticker is `BLOCKED_BY_DATA` (3/30 were this run).

## Session 2026-09-12 summary (this session)

Started from an E2E audit request: even the best G4_PASS candidates (4/30, ~9-12% risk_adjusted_er each) left the portfolio's blended return (~2.5%/yr, only ~24% of NAV ever deployed under the old risk-budget) nowhere near the ~18.6%/yr pace the $20k/24mo goal actually requires. That audit reframed the rest of the session:

1. **User redefined the mandate to a single objective** (see Objective above): reach/exceed the $ goal using available capital, with risk management serving that goal rather than competing with it, and required that new-buy proposals be evaluated against the whole current portfolio (8 real holdings: ADP, BRKB, GOOGL, RTX, SPGI, VEA, VIG, XLV), not in isolation.
2. **Data-completeness prerequisites, found while trying to risk-assess the current holdings**: a live YAML syntax bug (hotfixed immediately, was breaking all config loading), verified SEC-sourced identity fixes for PAGS (direct NYSE listing, no ADR) and UL (1:1 ADS) -- both confirmed working on real data, though PAGS separately surfaced a second bug (see open items); the ETF constituent-lookup budget (60 -> 300) that was starving IBB and likely other ETFs since the Fase 2 full-universe cutover; and `sector_overrides` for BRKB/SPGI/ADP/RTX/GOOGL, which had `industry_sector_official=None` and could never get a Bear case at all -- found a real subtlety along the way (BRKB's override needs to match its `underlying_ticker` "BRK/B", not just "BRKB"). All validated against real data before merging.
3. **`src/portfolio/allocation.py` redesigned**: `portfolio_stress_budget_nav` now conceptually covers the WHOLE portfolio (existing holdings + new buys), not new-deployment risk alone (existing_holdings_stress_nav param, not yet wired into production -- see open items); new-buy position sizing now scales with `diversification_score` (already computed against the real current holdings by `src/risk/portfolio_fit.py`) instead of every G4_PASS candidate getting the same flat per-position budget regardless of how correlated it is with what's already held. `portfolio_stress_budget_nav` raised 10% -> 30%. Confirmed on real data: the diversification multiplier correctly sizes NVDA/MSFT (higher diversification scores) up and GE (near-neutral) essentially unchanged.
4. **User flagged a further, deliberately-deferred architectural gap**: the scenario engine's 3 archetypes (CORPORATE/FINANCIALS/ENERGY) are too coarse against Comafi's real 13-category industry taxonomy -- see open items, next big topic.
5. **User questioned the `BLOCKED_BY_DATA` governance rule** ("¿por qué bloqueamos la asignación si hay un BLOCKED_BY_DATA? ¿un experto no sacaría ese ticker del Top-30 y listo?"). Investigation found this was a REAL bug, not just a design question: the veto was independently duplicated in **three** layers -- `build_g4_cash_hurdle.py::_govern` (`deployment_decision='NO_NEW_DEPLOYMENT_DATA_GAPS'` whenever `blocked_count>0`, checked *before* whether any clean G4_PASS candidate existed), `build_decisional_risk.py` (its own standalone `blocked_count>0` veto, redundant with G4's), and `build_investment_committee.py` (`elif blocked>0: HOLD_CASH_NO_ACTION` as a decision-terminal branch). All three fixed: a blocked ticker (a per-ticker data gap, e.g. PAGS's currency-normalization bug) is excluded from evaluation on its own -- as it always was -- but no longer vetoes deployment into other, cleanly-evaluated G4_PASS candidates. `ranking_status`/`secondary_considerations` still surface the gap for visibility. Methodology versions bumped: G4 unchanged (G4-2.1, only the governance wrapper changed, not the economic engine), Risk RISK-DECISIONAL-1.3 -> 1.4, Committee COMMITTEE-1.2 -> 1.3.
6. **Wired `existing_holdings_stress_nav` into production** (was the top open item from earlier in this session). New `_compute_existing_holdings_stress()` in `build_investment_committee.py` runs Deep Scenario Review against the broad-universe valuation (not just this week's Top-30) for every currently-held ticker, sums `weight * |bear-case % move|` for tickers whose scenario validates, and reports unassessed held tickers' weight separately (never defaults their risk to zero). `investment_committee.yml` now downloads `canonical-broad-valuation-scenarios` (via `research_run_id` from the same `weekly_screening.yml` run) and passes `--positions`/`--broad-valuation`/`--scenario-policy`.

**Both fixes validated together on real market data**, dispatched on the feature branch (`weekly_screening.yml` -> `valuation_scenarios.yml` with `dispatch_g4=true` -> G4 -> Risk -> Committee, all green, `FINAL_E2E: PASS`). Before today's fix, this exact week's data (PAGS still `BLOCKED_BY_DATA`, its currency bug unfixed) would have produced `HOLD_CASH_NO_ACTION` / `SCENARIO_DATA_GAPS_FAIL_CLOSED` regardless of anything else. After: **4/30 G4_PASS (NVDA, GE, MSFT, AMZN)**, PAGS still individually `BLOCKED_BY_DATA` (reported in `secondary_considerations`, no longer a veto), Committee decision **`CANDIDATES_REQUIRE_EXECUTION_GATE` / `G4_PASS_RISK_PASS`** -- the allocator actually executed for the first time this session:

| ticker | target_weight | diversification_multiplier | worst_case_stress_loss |
|---|---|---|---|
| NVDA | 7.30% | 1.1666 | 31.9% |
| MSFT | 6.81% | 1.1325 | 33.3% |
| AMZN | 6.09% | 1.0534 | 34.6% |
| GE | 5.88% | 0.9762 | 33.2% |

Portfolio-wide stress accounting: `existing_holdings_stress_contribution_nav`=6.17% (from the 6 currently-held positions Deep Scenario Review could actually assess: ADP/GOOGL/RTX/SPGI/VEA/XLV; BRKB and VIG remain unassessed -- see open items 4/6 -- and their 15.47% combined NAV weight is reported, not silently zeroed), `new_deployment_stress_contribution_nav`=8.66%, `total_portfolio_stress_contribution_nav`=14.83% vs. the 30% budget (`portfolio_stress_budget_ok: true`, real headroom remained). `goal_tracking`: required 18.65%/yr, this cycle's weighted new-deployment expected return 10.28%/yr -- still `NEW_DEPLOYMENT_BELOW_PACE` (expected; these were governance/wiring fixes, not new G4_PASS candidates or a threshold change, so they were never going to close the whole gap on their own).

Also discovered, and corrected in `PROJECT_STATE.md` itself: the "only validates with pinned lineage from `main`" note below was too pessimistic -- `weekly_screening.yml`/`valuation_scenarios.yml`'s upstream lookups are branch-scoped, but the upstream workflows themselves (`build_universe.yml`, `underlying_prices.yml`, `market_features.yml`, `cedear_local_market.yml`, `portfolio_state.yml`) are plain `workflow_dispatch` with no required inputs and (except `underlying_prices.yml`'s own dependency on `build_universe.yml`, and `market_features.yml`'s on `underlying_history.yml`, neither of which are branch-scoped either) no cross-branch requirements. Dispatching all 5 on the feature branch once unblocks a fully real, pinned-lineage dry run pre-merge without needing `main` at all.

## Session 2026-09-14 summary (this session)

Started directly from the prior session's top open item: sector taxonomy expansion. Scoped as "Fase A" (not all 13 Comafi categories at once, per the plan in `/root/.claude/plans/wondrous-marinating-muffin.md`), grounded in the REAL canonical universe's category distribution (pulled from a real `build_universe.yml` artifact, not just the theoretical 13-category list from Comafi's UI): Consumer Non-cyclical 43, Technology 28, Consumer Cyclical 25, Communications 25, Financial 22, Industrial 15, Basic Materials 13, Energy 12, Utilities 1, on 235 tickers.

1. **4 new archetypes added** (PR #25): `UTILITIES`, `BASIC_MATERIALS`, `CONSUMER_NON_CYCLICAL`, `CONSUMER_CYCLICAL` in `deep_scenario_engine.py`, each following the existing house style (fundamental-anchored base blended toward consensus median, mechanical bear blended toward consensus low, bull ordering floor) with real economic differentiation -- see PR #25's body for the full formula rationale per archetype. Technology/Communications/Industrial deliberately left on CORPORATE (no evidence of misfit; Fase B candidates). Existing `sector_overrides` (V/MA/ADP/RTX/GOOGL/SPGI/BRKB) untouched and still win over the new auto-classification. `methodology_version` stays `SCENARIO-2.2` (confirmed via `git log -S` this has never been bumped for an archetype-formula change in this project).
2. **Found and fixed a real, previously-undiscovered bug while validating against real data**: `build_valuation_scenarios.py`'s broad-universe output never carries `industry_sector_official` forward at all -- `valuation_scenarios.yml` re-merges it from the canonical universe for the Top-30 before Deep Scenario Review, but `_compute_existing_holdings_stress` (added last session, PR #24) didn't do the same for held tickers outside the Top-30. Confirmed empirically: without the merge, 190/199 real equity rows in the broad universe fail `SECTOR_CLASSIFICATION_UNVERIFIED`; with it, only 12 do. Fixed with a new optional `--universe` arg that performs the same merge; `investment_committee.yml` now downloads `canonical-universe` via `universe_run_id`.
3. **Validated on real production data**: full chain dispatched on the feature branch, `FINAL_E2E: PASS`. This week's real Top-30 sector distribution: CORPORATE 17, CONSUMER_CYCLICAL 3, FINANCIALS 2, CONSUMER_NON_CYCLICAL 2, non-equity 6 -- the new archetypes are firing on real tickers. G4_PASS: 4 (NVDA, LLY, AMZN, MSFT -- LLY replaced GE vs. two days prior, normal weekly variance). Committee: `CANDIDATES_REQUIRE_EXECUTION_GATE` / `G4_PASS_RISK_PASS`, confirming the PR #24 fix still works correctly alongside the new taxonomy. `existing_holdings_stress_metrics`: 19.24% assessed, 19.63% unassessed (BRKB, VEA, VIG) -- BRKB/VIG are the same known gaps as before; **VEA is newly unassessed this cycle**, which wasn't true two days ago -- see open items, worth a look if it recurs.
4. Also discovered along the way (documented in PROJECT_STATE.md's own working notes, not previously written up formally): `mcp__github__download_workflow_run_artifact`-style signed URLs can be fetched directly with `curl` from this environment, which made it possible to inspect real universe/valuation parquet data locally (not just via job logs) to ground the archetype design and verify the merge fix -- useful technique for any future real-data-grounded design work.

### VIG investigation (same session, continued)

Investigated why VIG (~9.3% of NAV, the largest current holding) was entirely absent from the canonical universe (open item from earlier this session).

1. **Root cause confirmed, not a code bug**: VIG's CEDEAR program genuinely does not appear in Comafi's or Caja de Valores' live catalogues -- the two official sources `fetch_official_universe_audit()` scrapes. Verified via direct `curl` fetches of both live pages and grepping for VIG's ticker, issuer name (VANGUARD DIVIDEND APPRECIATION ETF), ISIN (AR0625060907) and caja_code (8693): zero matches anywhere. VEA, a structurally identical Vanguard ETF on the same Comafi page, matches fine (`MATCH_BY_CAJA_CODE`), ruling out a parser bug. BYMA's product page was also checked but is a JS-rendered SPA with 0 static `<table>` elements -- even VEA doesn't appear in its raw HTML -- so it carries no evidential weight either way and was discounted.
2. **User independently confirmed via Balanz (their broker)** that they still hold an active VIG position, before any exception was applied -- per the project's fail-closed discipline, financial/identity data is never fabricated or assumed without this kind of independent confirmation.
3. **Fixed with the existing `mandate_exception` mechanism** (`config/universe_inclusions.yml`), the identical pattern already used for IWDA: `mandate_exception: true`, `mandate_exception_reason: INCLUDED_BY_USER_DECISION`, full sourced identity data (ISIN/caja_code from the 2026-09-07 research), documented with an explanatory comment block. Commit `58fd7d2` on `claude/modest-maxwell-xnwsfi`, not yet in a PR.
4. **Validated twice**: locally (installed `lxml`, ran `build_universe.py` directly against the real live Comafi/Caja audit -- `canonical_eligible_count` 235 -> 236, VIG's row now matches IWDA's pattern exactly) and via the full real production chain on the feature branch (`build_universe.yml` through `investment_committee.yml`, all green, `FINAL_E2E: PASS`).
5. **Found a second, distinct real issue while validating**: VIG now reaches Valuation with a real price ($239.43) but is `BLOCKED_BY_DATA` / `ETF_HOLDINGS_STALE` -- its primary Vanguard holdings source (`config/etf_issuer_sources.yml`, `max_age_days: 45`) apparently failed at runtime, falling back to a secondary `StockAnalysis` source whose snapshot (dated 2026-06-30, 76 days old) exceeds the freshness policy. Confirmed in the latest `investment_committee.yml` run (`34848132510`): VIG moved from "absent from universe entirely" to "present, but still `existing_holdings_unassessed_tickers` alongside BRKB and VEA" -- an improvement (it's now counted and visible) but not a full fix. Not yet actioned; candidate follow-up, needs its own investigation into why the Vanguard primary source is failing.

### ETF_HOLDINGS_STALE root cause and fix (same session, continued)

Investigated why VIG's (and VEA's) Vanguard primary source was failing at runtime, per the user's follow-up request.

1. **Root cause**: Vanguard restructured their site. The JSON API `_fetch_vanguard_json` called (`investment-products/etfs/profile/api/{ticker}/portfolio-holding/stock`) now 301-redirects to Vanguard's Angular app shell (`text/html`, not JSON). The HTML-table/embedded-state fallback in `_fetch_vanguard` was also silently broken: Vanguard's advisor pages are now fully client-rendered via web components (`funds-web-components.cdn.web.vanguard.com`), with zero holdings data in the raw server HTML -- confirmed empirically (0 `<table>` elements, 0 ticker mentions in the raw fetched page). Both of `_fetch_vanguard`'s two paths were dead; the code correctly fell through to the stale secondary as designed, it just had nothing better to fall back from.
2. **Found the replacement by reverse-engineering Vanguard's own JS bundle** (`funds-web-components.cdn.web.vanguard.com/portfolio-composition.js` + its shared `common-*.js` chunk): `investor.vanguard.com/irr/funds/profile/{ticker}-AdditionalFundData` returns the same `holdingDetails` payload the profile page renders from, just reshaped (`equityHoldings[].ticker`/`marketValuePercentage`, `asOfDate` as `MM/DD/YYYY` instead of the old shape's `fund.entity[].percentWeight`/ISO date). Confirmed it works with the ticker directly (no need to resolve Vanguard's internal numeric fund ID).
3. **Fixed**: `_fetch_vanguard_json` now calls the new endpoint; parsing logic split into a new pure `_parse_vanguard_additional_fund_data` staticmethod (testable without network, same pattern as `_normalize_table`). 4 new unit tests in `tests/test_vanguard_holdings_parser.py`.
4. **Validated live and via the full production chain**: locally, both VIG (313 holdings, `as_of=2026-07-31`) and VEA (1765 holdings, same date) now fetch from `ISSUER_OFFICIAL` tier, not the fallback. Full chain dispatched on the feature branch (`weekly_screening.yml` -> `valuation_scenarios.yml` -> G4 -> Risk -> Committee, commit `0f68eea`): VIG's `valuation_status` is now `VALUATION_READY` (was `BLOCKED_BY_DATA`); `ready_count` 199 -> 222/236. `existing_holdings_stress_metrics.existing_holdings_unassessed_tickers` went from `["BRKB", "VEA", "VIG"]` (19.63% unassessed) to just `["BRKB"]` (6.36% unassessed) -- VEA and VIG are both now fully assessed. `assessed_weight` 19.24% -> 32.52%. Committee decision unchanged (`CANDIDATES_REQUIRE_EXECUTION_GATE` / `G4_PASS_RISK_PASS`, same 4 G4_PASS names), as expected -- this fix only touches data freshness, not G4/Risk/allocator logic.
5. **Note for future reference**: VIG's `asOfDate` (2026-07-31) is exactly 45 days old as of 2026-09-14 -- right at, not over, its 45-day policy cap. Vanguard refreshes holdings roughly monthly, so this will likely recur near each cap boundary; not a data problem to fix preemptively, just worth knowing if VIG flips back to `ETF_HOLDINGS_STALE` on a slow week -- check whether Vanguard has actually stopped publishing before assuming the connector broke again.

### BRKB dual-class EPS/PE fix (same session, continued)

Investigated the last remaining gap in `existing_holdings_stress_metrics`, per the user's follow-up request ("Dale con BRKB, a ver ese bug de dual-class").

1. **Root cause confirmed against real data**: pulled BRKB's real row from today's broad-valuation artifact. Finnhub's `/stock/metric` reports `fundamental_eps_normalized` (46,570.24), `fundamental_pe_normalized` (14.6701) and `fundamental_book_value_per_share` (497,171.86) at Class A economic scale even though the connector correctly queries the BRK.B provider symbol (`_provider_symbol` already maps `BRKB`/`BRK/B` -> `BRK.B`, so this isn't a symbol-resolution bug) -- while `current_price` ($510.37) is genuine BRK.B market price. Implied Class A fair value (eps*pe) is ~$683,262, a ~1339x mismatch -- exactly what `ECONOMIC_IDENTITY_EPS_PE_UNIT_MISMATCH` was correctly catching. The independent book-value/price-to-book check confirms the same ~1500x scale (implied ~$752,469), ruling out a one-off fluke in a single field.
2. **Verified the real conversion ratio before using it** (never guessed): Berkshire's Class A-to-B share ratio is 1,500:1, public and SEC-documented (10-K "Description of Registrant's Securities"), unchanged since the 2010 Class B split.
3. **Fixed with a new mechanism parallel to the existing ADR-ratio one**: `verified_domestic_dual_class_ratios` in `config/scenario_review_policy.yml`'s `identity` block (distinct from `verified_adr_ratios`/`verified_direct_foreign_listings` so a domestic dual-class stock doesn't get mislabeled with an ADR/foreign-listing flag -- new `identity_security_type=DOMESTIC_DUAL_CLASS` and `VERIFIED_DOMESTIC_DUAL_CLASS_RATIO_APPLIED` flag instead). `_verified_security_mapping` in `deep_scenario_engine.py` gets a third lookup branch; BRKB/BRK/B entries use `reference_class_shares_per_traded_share: 1/1500`.
4. **Validated locally against BRKB's real row**: identity check now passes (`VERIFIED_NORMALIZED`, implied price ~$455.5, ratio ~0.89), sector still resolves to FINANCIALS via the existing override (untouched), and `validate_scenarios` produces a full, plausible Bear ($355.56, -30.3%) / Base ($464.06, -9.1%) / Bull ($534.50, +4.7%) scenario.
5. **Validated via the full production chain** (commit `b8e7d9a`): `existing_holdings_unassessed_tickers` went from `["BRKB"]` to `[]` -- **all 8 held positions are now fully assessed for the first time this project**. `existing_holdings_assessed_weight` 32.52% -> 38.87%. Committee decision unchanged (`CANDIDATES_REQUIRE_EXECUTION_GATE` / `G4_PASS_RISK_PASS`, same 4 G4_PASS names), as expected.

### Sector taxonomy Fase B: investigated, evidence does not support it (same session, continued)

User asked to proceed with Fase B (Technology 28 / Communications 25 / Industrial 15 tickers, all currently CORPORATE). Per the plan's own bar ("only take this on if real data suggests otherwise"), pulled real broad-valuation + universe data and ran every ticker in these 3 categories through `validate_scenarios` under the current CORPORATE model before writing any new code.

- **Technology**: no evidence of misfit -- these are exactly the growth/megacap names (AAPL, MSFT, NVDA, etc.) CORPORATE's growth-adjusted-PE cap was already built and validated for.
- **Communications**: heterogeneous (legacy carriers T/VZ/TMUS mixed with GOOGL/META/NFLX/AMZN), but the carriers' Debt/Equity leverage stress is modest (2-6 extra points, nowhere near the 12-point cap) and their Bear/Base/Bull outputs look reasonable. No clear distortion.
- **Industrial**: a real signal -- CAT, DE and UNP hit at or near the leverage-stress cap (D/E ~2-2.5x, structural from captive-finance arms / regulated rail infrastructure, similar in spirit to the Utilities finding in Fase A) -- but the same bucket also has Boeing (D/E ~9.9x, genuine distress, not structural) and 3M (real asbestos/PFAS litigation risk). A blanket "no leverage stress" archetype for all of Industrial, the same shape of fix that worked cleanly for Utilities, would under-penalize the names where the leverage really is a risk signal here. Not a clean case -- the evidence is real but mixed, not uniform.

**Decision: did not build new archetypes.** Recommended against forcing Fase B on weak/mixed evidence, per the user's own standing rule about suspecting when a fix is being reached for without solid grounding; the user agreed and redirected to the 3 real identity bugs found along the way (HON/TEN/ERIC, see below) instead.

### HON / TEN / ERIC identity-mismatch investigation (same session, continued)

While running Fase B's real-data check, 3 tickers came back `ECONOMIC_IDENTITY_EPS_PE_UNIT_MISMATCH`-blocked for reasons unrelated to sector taxonomy. Investigated each on its own merits per the user's request ("Dale con HON, TEN y ERIC").

1. **TEN (Tenaris) -- fixed, a real ADR ratio.** Real data: `eps*pe/price` = 0.499 (just below the 0.55 floor) with no ADR adjustment. Verified via Tenaris's own investor-relations page (confirmed 1 ADS = 2 ordinary shares, unchanged since a 2006 board-approved change from the original 10:1, also documented in Tenaris's SEC Form 6-K FY2006) before using it -- not a guessed multiplier. Added `TEN`/`TS` to `verified_adr_ratios` (`ordinary_shares_per_ads: 2`). Applying it brings the ratio to 0.999, essentially a perfect match -- strong internal confirmation the sourced ratio is correct, not just externally documented.
2. **HON (Honeywell) -- fixed, an architectural gap, not a data/ratio issue.** Real data: EPS/PE ratio 0.493 (just below the floor), but the independent book-value/price-to-book ratio is 0.964 -- cleanly inside the band. HON is purely domestic, single-class -- no ADR or dual-class ratio applies here, so this isn't a "missing conversion factor" problem; it's EPS-metric definitional noise (normalized vs. trailing EPS, plausibly from Honeywell's recent Solstice Advanced Materials spinoff) that a real balance-sheet cross-check already resolves. `_identity_check` now corroborates with PB before blocking on a borderline EPS/PE miss (new `ECONOMIC_IDENTITY_PB_CORROBORATION_RESCUES_EPS_PE_MISS` flag) -- confirmed this can't silently rescue a genuine mismatch: BRKB pre-fix, TEN pre-fix, and ERIC (below) all fail BOTH ratios together, not just one, so the rescue only ever fires when there's real independent corroboration.
3. **ERIC (Ericsson) -- root-caused, NOT fixed: needs a live FX-rate mechanism, same class of problem as PAGS.** Real data: both EPS/PE ratio (9.41) and PB ratio (8.79) come out ~9-9.4x too high, consistently -- not the shape of an ADR/share-ratio problem (those show as clean small integer multiples like TEN's 2x), but the shape of a currency mismatch. Checked the real USD/SEK rate for today (2026-09-14): ~9.70, matching almost exactly. Ericsson's `/stock/metric` fundamentals are very likely reported in SEK while the ADR trades in USD. Unlike TEN's fixed corporate ratio, USD/SEK floats daily -- hardcoding today's rate into policy config would go stale immediately and violates the project's own "never guess/fabricate financial data" discipline. Needs a real, live FX-rate data source wired into `fundamental_fx_to_market` (already an existing field the identity check reads, just never populated for either PAGS or ERIC) -- a genuinely bigger piece of work than a config entry, deferred rather than rushed. See open item below.
4. **Validated locally against real data**: HON and TEN both now pass identity and produce full Bear/Base/Bull scenarios (`scenario_validated=True`). ERIC still correctly fails closed (`ECONOMIC_IDENTITY_EPS_PE_UNIT_MISMATCH`) -- no change in behavior since no real fix was applied.

## Open items for next session (updated 2026-09-14)

1. ~~Sector taxonomy Fase B~~ -- **investigated, not pursued**: real data doesn't support building new archetypes for Technology/Communications/Industrial (Technology fits CORPORATE fine, Communications' leverage effect is modest, Industrial's is real but too heterogeneous -- Boeing/3M's leverage is genuine risk, not structural like CAT/DE/UNP's). See "Sector taxonomy Fase B: investigated, evidence does not support it" above. Don't re-attempt without new evidence.
1b. **Live FX-rate mechanism needed for PAGS and ERIC.** Both show a currency-shaped `ECONOMIC_IDENTITY_EPS_PE_UNIT_MISMATCH` (fundamentals reported in the issuer's home currency -- BRL for PAGS, SEK for ERIC -- while trading in USD), and both need `fundamental_fx_to_market` populated from a real, live-updated source (not a hardcoded config ratio -- unlike an ADR ratio, an FX rate isn't a fixed corporate fact). This is a genuinely bigger piece of work (a new connector/data source, not a policy-config entry) -- scope it as its own session rather than rushing a guessed rate into either ticker.
2. ~~VEA `unassessed` in `existing_holdings_stress_metrics`~~ -- **fixed same session**: same root cause as VIG's `ETF_HOLDINGS_STALE` (Vanguard's JSON API URL changed), not a one-off. See "ETF_HOLDINGS_STALE root cause and fix" above.
4. **Portfolio snapshot reconciliation, blocked on the user.** `data/input/portfolio_state_baseline_20260912.json` exists but is NOT wired into `portfolio_state.yml` (still points to the 20260904 baseline) because the 8 per-ticker dollar values given summed to $20,063, not the stated CEDEAR total of $19,080.36 (~$983 gap). User confirmed there's a typo on one line and will send fresh, precise data from Balanz (their broker) once account maintenance there finishes -- do not guess which number is right in the meantime.
5. ~~BRKB's dual-class EPS/PE unit mismatch~~ -- **fixed same session**. See "BRKB dual-class EPS/PE fix" above. `existing_holdings_unassessed_tickers` is now empty -- all 8 held positions are fully assessed.
6. ~~PAGS's second-order identity issue~~ -- **consolidated into item 1b above**: confirmed same root cause class as ERIC (currency mismatch, BRL vs. USD), needs the same live FX-rate mechanism. Not fixed.
7. ~~VIG's `ETF_HOLDINGS_STALE` / `BLOCKED_BY_DATA` valuation gap~~ -- **fixed same session** (both the universe-absence gap, PR #27 merged, and this one). See "ETF_HOLDINGS_STALE root cause and fix" above. VIG is `VALUATION_READY` and out of `existing_holdings_unassessed_tickers` as of the latest validated run. Only BRKB remains unassessed (item 5, a separate dual-class EPS bug).
8. **Decide the fate of the shadow workflows** (`value_research_shadow.yml`, `value_research_shadow_g4.yml`, `src/orchestration/build_value_research_shadow.py`) — their whole purpose (compare shadow value-first vs. production technical-only) is moot now that production *is* value-first. Still not done; also disable/reconsider the Friday-22:00-UTC cron on `value_research_shadow.yml`.
9. Re-run the full chain periodically (or watch the "Weekly CEDEAR pipeline check-in" Routine, next fires 2026-09-18) to see how the PASS-candidate set evolves and to catch new data gaps over time rather than only on manual dispatch.
10. No FX/regulatory or bear-downside threshold work is pending — those are considered settled (see permanent safeguards below for why they were loosened, so they aren't re-tightened by mistake without re-running `g4_sensitivity_audit.py` first).

## Important design lessons / permanent safeguards

- Never hardcode universe size.
- Never rerun an old failed workflow expecting it to use a newer workflow definition; GitHub re-runs execute the workflow definition associated with the original run/commit.
- Manual `workflow_dispatch` of G4 chooses the latest successful Valuation run; confirm it belongs to the desired Research lineage.
- Symbol aliases are an identity-resolution problem, not merely a string-replacement problem.
- One-to-many matches must fail closed rather than be silently deduplicated.
- Research Top-30 is immutable downstream; downstream stages may classify/block candidates but must not substitute names.
- Keep `NO_NEW_DEPLOYMENT`/`HOLD_CASH_NO_ACTION` unless the full verified downstream process supports deployment.
- **A per-name absolute veto/margin is easy to justify individually but tends to duplicate a risk already priced elsewhere or already handled at the portfolio level** (this session found and fixed this pattern four separate times: Bear downside veto, FX-stress veto, `minimum_margin_over_hurdle`, `dynamic_equity_margin`'s buffers). When G4 rejects everything, check for double-counting before loosening a threshold.
- **The selection stage (which names even reach Valuation/G4) can matter more than the valuation methodology itself.** All the archetype/probability/margin fixes this session only closed part of the gap; the Fase 2 selection-order cutover is what actually produced PASS candidates, because the old technical-momentum screen was excluding the right names entirely, upstream of any G4 logic.
- `weekly_screening.yml` / `valuation_scenarios.yml` resolve their upstreams (`build_universe.yml`, `market_features.yml`, `underlying_prices.yml`, `cedear_local_market.yml`, `portfolio_state.yml`) filtered by the triggering branch (`gh run list --branch "${GITHUB_REF_NAME}"`) — so a feature branch that never had its own successful runs of those five will fail with `ValueError: invalid literal for int() with base 10: ''` (an empty run id). **This is not a `main`-only limitation** (corrected 2026-09-12; an earlier version of this note said otherwise): all five are plain `workflow_dispatch` with no required inputs, so dispatching them once on the feature branch (in any order — the ones with their own upstream dependencies, `underlying_prices.yml` on `build_universe.yml` and `market_features.yml` on `underlying_history.yml`, resolve those via an unfiltered `gh run list`, i.e. latest successful run on ANY branch) unblocks a fully real, pinned-lineage dry run of the whole production chain pre-merge, without needing `main` at all.

## Do not regress to

- 316/305/304 (or any other) historical universe-size assumptions — the count is dynamic.
- deleting problematic G4/Research candidates to make 30/30 pass.
- generic token matching for BYMA/Caja identity.
- unpinned or mixed lineage between Research, Valuation, G4, Risk and Committee.
- re-tightening `max_bear_downside` back toward -15% or restoring the FX-stress individual veto without re-running `g4_sensitivity_audit.py` against current data first — both were loosened this session based on real evidence that they rejected virtually every real candidate, not because risk no longer matters (the risk is still priced, just at the portfolio/allocator level instead of a per-name absolute gate).
- reverting Research to technical/momentum-only Top-30 selection — this was the root cause of G4 never finding a candidate all session; see Fase 2 above.
